# 6. Оновлення

Розпакуйте нову версію та виконайте:

```bash
sudo ./upgrade.sh
```

`upgrade.sh` повторно запускає idempotent installer.

Збережені caches перезаписуються лише після успішної генерації та перевірки JSON.
